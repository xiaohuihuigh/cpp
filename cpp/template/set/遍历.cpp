#include<bits/stdc++.h>
using namespace std;
int main(){
    set<int > S;
    int t;
    while(cin>>t){
        S.insert(t);
        S.const_iterator
    }
    for(set<int >::iterator iter = S.begin();iter != S.end();iter++){
        cout<<*iter<<endl;
    }
}
